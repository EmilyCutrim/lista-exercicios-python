<p align="center">
  <img src="https://iili.io/fzGmfBS.png" alt="Emily Keren">
</p>

## 🚀 PROJETO PESSOAL - Coleção de Fundamentos em Python 🐍

#### 📖 Descrição:
Este repositório é uma **coleção de exercícios e scripts** focados na consolidação dos **fundamentos da linguagem Python**, seguindo rigorosamente as diretrizes da **PEP 8** para boas práticas de código.

O projeto está estruturado em **módulos temáticos** (packages) para demonstrar a aplicação prática de conceitos essenciais, como:

* **Estruturas Condicionais:** Uso de `if`, `elif`, e `else` para tomada de decisão.
* **Operadores Aritméticos e Lógicos:** Implementação de cálculos e comparações.
* **Manipulação de Variáveis e Strings:** Conversão de tipos (`int()`, `float()`), e uso de métodos de string (`.strip()`, `.lower()`, `.upper()`, `.replace()`).
* **Entrada e Saída de Dados (I/O):** Coleta de dados com `input()` e exibição com `print()`.
---
#### Métodos de execução:

### 🖥️ **Rodar Localmente**
Requisitos para execução:

##### 🔍 Download e Configuração do Python localmente:

1. Acesse o site oficial do Python e baixe o instalador mais recente para o seu sistema operacional (Windows, macOS ou Linux);
2. Executar o Instalador atentando-se em marcar a caixa (**"Adicionar python.exe ao Path"**) para permitir que possamos execute o Python a partir de qualquer diretório no seu Prompt de Comando ou Terminal;
3. Verificar a Instalação abrindo o Terminal ou Prompt de Comando e execute o seguinte comando:
   
```bash
python --version
```

Deve ser exibido uma mensagem informando algo como: "Python 3.11.5". Isso representa que a instalação foi bem-sucedida. Caso contrário, consulte as configurações do seu PATH.

4. Em seguida, é necessário Instalar/Atualizar o PIP que é o gerenciador de pacotes do Python. Sendo ele essencial para instalar bibliotecas. Obs: Ele geralmente é instalado junto com o Python, mas podemos usar para garantir que está atualizado.
```bash
python -m pip install --upgrade pip  
```
✅ Feito isso, apenas faça um clone do projeto em sua máquina; abra na IDE que melhor preferir e execute o arquivo desejado (ex: variaveis/conversor_maiusculo.py).

No terminal, o fluxo de execução dependerá do script escolhido e das entradas fornecidas, exemplo:

01. O script solicitará o primeiro valor: Digite o primeiro valor:

02. O script solicitará o segundo valor: Digite o segundo valor:

03. O script solicitará o terceiro valor: Digite o terceiro valor:

04. Por fim, o resultado da soma será exibido, por exemplo: O resultado da soma é 45

<p align="center">
  <img src="https://iili.io/fzVltLu.png" alt="Representação de terminal">
</p>

#### 🛠️ Tecnologias utilizadas:
- 🐍 Python 3.13.9;
---

## 🌸 Autora
<table>
  <tr>
    <td align="center">
      <a href="https://www.linkedin.com/in/emily-cutrim-81561839a/" title="Emily Cutrim">
        <img src="https://github.com/user-attachments/assets/546a5020-9127-45e3-9cb4-000f2a66167f" width="190px;" alt="Foto do Emily Cutrim"/><br>
        <sub>
          <b>Emily Cutrim</b>
        </sub>
      </a>
    </td>
  </tr>
</table>
